# tag 发布触发（无条件，保证打 tag 一定构建并推送 :<tag>）
# 不受 paths 过滤，确保发布可靠
name: Release (tag)

on:
  push:
    tags: ['v*']

jobs:
  build:
    uses: ./.github/workflows/build-images.yml
    with:
      push: true
